# 第二阶段优化规划

## 目标

第二阶段继续做低风险、可验证的稳定性优化，优先减少构建警告和潜在空引用，不改变现有 UI 交互。

## 已完成事项

- 修正 `RelayCommand` 可空性。
- 加固 `App.xaml.cs` 启动和托盘菜单空值处理。
- 加固开机自启注册表逻辑。
- 常量化更新检查配置并补充响应校验。
- 运行 `dotnet build` 验证通过。

## 结果

项目构建已达到 0 个警告、0 个错误。后续可继续拆分服务、优化 ViewModel 绑定，并评估托盘依赖替换。
